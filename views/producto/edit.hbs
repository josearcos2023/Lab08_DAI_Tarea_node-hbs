<!DOCTYPE html>
<html>
    <head>
        <title>Editar Producto</title>
    </head>
    <body>
        <div class="container">
            <h3><a href="/productos">Lista de productos</a></h3>
            <h1>Editar Producto</h1>
            <form action="/productos/update/{{ producto._id }}" method="post">
                <table>
                    <tbody>
                        <tr>
                            <td>Código</td>
                            <td><input type="text" name="codigo" value="{{ producto.codigo }}" /></td>
                        </tr>
                        <tr>
                            <td>Nombre</td>
                            <td><input type="text" name="nombre" value="{{ producto.nombre }}" /></td>
                        </tr>
                        <tr>
                            <td>Tipo</td>
                            <td><input type="text" name="tipo" value="{{ producto.tipo }}" /></td>
                        </tr>
                        <tr>
                            <td>Precio</td>
                            <td><input type="number" name="precio" value="{{ producto.precio }}" /></td>
                        </tr>
                        <tr>
                            <td colspan="2"><button type="submit">Update</button></td>
                        </tr>
                    </tbody>
                </table>
            </form>
        </div>
    </body>
</html>
